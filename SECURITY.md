# 安全说明

## 部署建议

- Compose 部署强制要求登录密码和会话密钥。服务可以在局域网内访问，但不要把端口直接映射到公网；需要远程访问时请使用 HTTPS 反向代理，见[安装指南](docs/getting-started.md#远程访问)。
- 以非 root 账号运行（`.env` 中的 `PUID` / `PGID`），只挂载需要的目录。
- 为 qBittorrent 设置独立的密码，并使用单独的下载分类和暂存目录。
- `.env`、`data/` 和备份中含有密码、密钥和下载器凭据，MissAV 的浏览器数据卷含有站点 Cookie，都不要公开。

## 报告安全问题

请通过 GitHub 仓库的私密安全报告（Security → Report a vulnerability）联系维护者，不要在公开 Issue 中描述漏洞细节；如果该入口尚未开放，请先在 Issue 中询问联系方式，不附带任何细节。任何报告中都不要粘贴密码、会话 Cookie、访问令牌、带签名的媒体链接或未脱敏的配置。

已经泄露的密钥应立即吊销并重新生成；删除文件不会清除 Git 历史、缓存或他人已下载的副本。
